SELECT * FROM DEPT,EMP;
This cmd gives a cartesian prod of both tables. 14*4 rows are displayed.

JOINS


CROSS JOINS:

SELECT * FROM DEPT,EMP


EQUI JOINS:

SELECT EMPNO, ENAME, JOB, EMP.DEPTNO, DNAME, LOC, DEPT.DEPTNO FROM DEPT, EMP
WHERE EMP.DEPTNO = DEPT.DEPTNO;

SELECT EMPNO, ENAME, JOB, DNAME, LOC, DEPT.DEPTNO FROM DEPT, EMP
WHERE EMP.DEPTNO = DEPT.DEPTNO;

SELECT EMPNO, ENAME, JOB, EMP.DEPTNO, DNAME, LOC FROM DEPT, EMP
WHERE EMP.DEPTNO = DEPT.DEPTNO;


NON EQUI JOINS:

SELECT EMPNO, ENAME, JOB, GRADE FROM  EMP, SALGRADE
WHERE SAL BETWEEN LOSAL AND HISAL;


OUTER JOINS:
ORACLE:
SELECT EMPNO, ENAME, JOB, DNAME, LOC FROM DEPT, EMP
WHERE EMP.DEPTNO (+) = DEPT.DEPTNO; --------------------------------- Right join

SELECT EMPNO, ENAME, JOB, DNAME, LOC FROM DEPT, EMP
WHERE EMP.DEPTNO = DEPT.DEPTNO (+); --------------------------------- Left join

SELECT EMPNO, ENAME, JOB, DNAME, LOC FROM DEPT, EMP
WHERE EMP.DEPTNO (+) = DEPT.DEPTNO;
UNION
SELECT EMPNO, ENAME, JOB, DNAME, LOC FROM DEPT, EMP
WHERE EMP.DEPTNO  = DEPT.DEPTNO(+); --------------------------------- full join

ANSI:
SELECT EMPNO, ENAME, JOB, DNAME, LOC 
FROM EMP
LEFT JOIN DEPT ON EMP.DEPTNO = DEPT.DEPTNO;

SELECT EMPNO, ENAME, JOB, DNAME, LOC 
FROM EMP
RIGHT JOIN DEPT ON EMP.DEPTNO = DEPT.DEPTNO;

SELECT EMPNO, ENAME, JOB, DNAME, LOC 
FROM EMP
FULL JOIN DEPT ON EMP.DEPTNO = DEPT.DEPTNO;


SELF JOINS:

select w.ename || ' is working as ' || w.job || ' and reporting to ' || m.ename || ' working as ' || m.job from emp m, emp w where w.mgr = m.empno;

INNER JOIN IS SAME AS EQUI JOIN

SET OPERATORS:
Union:
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=5
UNION
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=7;

Union all:
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=5
UNION ALL
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=7;

Intersect
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=5
INTERSECT
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=7;

Minus
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=7
MINUS
SELECT ROWID, ROWNUM, ENAME, JOB, DEPTNO FROM EMP WHERE ROWNUM<=5;